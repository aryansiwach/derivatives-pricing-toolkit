venv/
__pycache__/
*.pyc
.ipynb_checkpoints/
.pytest_cache/
*.egg-info/
.env

# licensed WRDS/OptionMetrics data -- never redistributed
data/

# results/ (aggregate tables + summary figures) IS committed: it is the
# analysis output, not the underlying data, and lets the repo be read without
# a WRDS session. Regenerate with the notebooks + scripts/build_results.py.

# local scaffolding
WRDS-MIGRATION-BRIEF.md
derivatives-pricing-toolkit-BUILD-BRIEF.md
